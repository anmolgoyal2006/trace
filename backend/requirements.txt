# Unified Trace backend dependencies
# Pin to known-good versions for reproducibility

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.31.0

# Database (async SQLite)
sqlalchemy==2.0.30
aiosqlite==0.20.0

# Pydantic + settings
pydantic==2.7.1
pydantic-settings==2.3.1

# File upload + form parsing
python-multipart==0.0.9

# HTTP client (for internal healthchecks if needed)
httpx==0.27.0

# AI pipeline deps (already in ai_pipeline/requirements.txt but listed here for one-shot install)
# Ultralytics 8.3+ fixes PyTorch 2.6 weights_only compatibility
# (the weights_only=False shim in detect.py/track.py stays as a safety net
# for older installs); lapx is required by the BoT-SORT tracker.
ultralytics>=8.3.0
lapx>=0.5.2
torch>=2.0.0
torchvision>=0.15.0
torchreid>=0.2.5
Pillow==10.3.0
opencv-python==4.9.0.80
numpy==1.26.4
PyYAML==6.0.1

# Utilities
loguru==0.7.2
